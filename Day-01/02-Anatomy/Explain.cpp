#include <iostream>   // (a) imports input/output tools
using namespace std;  // (b) lets you write cout instead of std::cout

int main() {           // (c) every program starts execution here
    cout << "text";    // (d) cout = "console output", << sends data to it
    return 0;           // (e) tells the OS the program finished successfully
}