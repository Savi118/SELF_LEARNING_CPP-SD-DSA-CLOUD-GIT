1. Check if a number is even or odd using %
2. Find the largest of 3 numbers using if-else if
3. Check if a year is a leap year (hint: divisible by 4, but not by 100 unless also by 400)
4. Build a simple calculator using switch — take two numbers and an operator (+, -, *, /) as input
5. Print numbers 1 to N using a for loop
6. Print the sum of digits of a number using a while loop (e.g., 123 → 1+2+3 = 6)
7. Reverse a number using a loop (e.g., 123 → 321) — no strings allowed, pure math
8. Check if a number is prime (loop from 2 to n-1, check divisibility)
9. Print a right-angled triangle pattern of stars using nested loops:
*
**
***
****
10. Print a multiplication table (1 to 10) for a user-input number using a for loop.

# For answer check the Q-no for that question solution.